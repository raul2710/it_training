{
  "name": "it-training",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IT Training - Questionários das aulas (Vite + HTML + CSS + JS vanilla)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}