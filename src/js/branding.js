import logoUrl from '../assets/images/logo.svg';

/**
 * Identidade visual centralizada.
 * Para usar sua própria logo, substitua o arquivo em src/assets/images/
 * e ajuste o caminho abaixo (aceita PNG ou SVG).
 */
export const BRANDING = {
  name: 'IT Training',
  logo: logoUrl
};
